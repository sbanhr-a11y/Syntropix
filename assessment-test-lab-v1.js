(()=>{'use strict';const API='https://syntropix-backend.onrender.com/api',params=new URLSearchParams(location.search),token=params.get('testToken');function load(){const s=document.createElement('script');s.src='/assessment-runtime-v3.js';s.onerror=()=>{document.body.innerHTML='<main class="runtime"><h1>Assessment runtime unavailable</h1></main>'};document.body.appendChild(s)}if(!token){load();return}const originalFetch=window.fetch.bind(window),oldUser=localStorage.getItem('syntropix_user'),oldToken=localStorage.getItem('syntropix_token');let active=false;function restore(){if(!active)return;if(oldUser===null)localStorage.removeItem('syntropix_user');else localStorage.setItem('syntropix_user',oldUser);if(oldToken===null)localStorage.removeItem('syntropix_token');else localStorage.setItem('syntropix_token',oldToken);active=false}window.addEventListener('pagehide',restore,{once:true});(async()=>{try{const r=await originalFetch(`${API}/command/test-lab/validate`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token}),cache:'no-store'}),d=await r.json();if(!r.ok||!d.valid)throw new Error(d.message||'Invalid test session.');active=true;localStorage.setItem('syntropix_user',JSON.stringify({role:'demo',name:'Syntropix Test Lab'}));localStorage.setItem('syntropix_token','TEST-LAB-EPHEMERAL');window.fetch=async(input,init={})=>{const url=typeof input==='string'?input:input?.url||'';if(url.includes('/payments/grant-bypass'))return new Response(JSON.stringify({status:'success',entitlementToken:'TEST-LAB-EPHEMERAL'}),{status:200,headers:{'Content-Type':'application/json'}});if(url.includes('/reports/submit'))return new Response(JSON.stringify({status:'success',testMode:true,persisted:false}),{status:200,headers:{'Content-Type':'application/json'}});if(url.includes('/validation/submit')||url.includes('/assessments/validation-response'))return new Response(JSON.stringify({status:'success',collected:false,reason:'internal-test-mode'}),{status:200,headers:{'Content-Type':'application/json'}});return originalFetch(input,init)};const banner=document.createElement('div');banner.setAttribute('role','status');banner.style.cssText='position:sticky;top:0;z-index:10000;padding:10px 18px;background:#D6B878;color:#090b09;text-align:center;font:800 12px/1.3 system-ui;letter-spacing:.08em';banner.textContent='INTERNAL TEST MODE · NO PAYMENT · ASSESSMENT DATA & REPORT NOT SAVED';document.body.prepend(banner);load()}catch(e){restore();document.body.innerHTML=`<main class="runtime"><section class="panel"><p class="kicker">TEST LAB</p><h1>Test session unavailable</h1><p>${String(e.message||'This launch link is invalid or expired.').replace(/[&<>]/g,'')}</p><p>Return to Syntropix Command and launch a fresh test.</p></section></main>`}})();})();
