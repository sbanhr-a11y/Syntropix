(()=>{const init=()=>{const nav=[...document.querySelectorAll('.home-desktop-nav a,.home-menu-panel a')];const p=(location.pathname.split('/').pop()||'index.html').toLowerCase();nav.forEach(a=>{const h=(a.getAttribute('href')||'').split('#')[0],t=(h.split('/').pop()||'index.html').toLowerCase();if(t===p)a.setAttribute('aria-current','page')});const els=[...document.querySelectorAll('.sx-reveal')];if(!('IntersectionObserver'in window)||matchMedia('(prefers-reduced-motion: reduce)').matches){els.forEach(e=>e.classList.add('visible'))}else{const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -8% 0px'});els.forEach(e=>io.observe(e))}

document.querySelectorAll('[data-testimonial-carousel]').forEach(carousel=>{const track=carousel.querySelector('.sx-testimonial-track'),cards=[...carousel.querySelectorAll('.sx-testimonial-card')],prev=carousel.querySelector('[data-carousel-prev]'),next=carousel.querySelector('[data-carousel-next]'),dots=[...carousel.querySelectorAll('.sx-carousel-dots i')];if(!track||!cards.length)return;let index=0,timer=null;const setDot=()=>dots.forEach((d,i)=>d.classList.toggle('active',i===index));const go=i=>{index=(i+cards.length)%cards.length;const target=Math.max(0,cards[index].offsetLeft-track.offsetLeft);track.scrollTo({left:target,behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});setDot()};prev?.addEventListener('click',()=>go(index-1));next?.addEventListener('click',()=>go(index+1));const start=()=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;clearInterval(timer);timer=setInterval(()=>go(index+1),6500)};const stop=()=>clearInterval(timer);track.addEventListener('scroll',()=>{const left=track.scrollLeft;let best=0,dist=Infinity;cards.forEach((card,i)=>{const d=Math.abs(card.offsetLeft-left-track.offsetLeft);if(d<dist){dist=d;best=i}});index=best;setDot()},{passive:true});carousel.addEventListener('mouseenter',stop);carousel.addEventListener('mouseleave',start);carousel.addEventListener('focusin',stop);carousel.addEventListener('focusout',start);setDot();start()})};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init,{once:true}):init()})();